# 회원가입 페이지 템플릿 시안 12

## 콘셉트

**Icon Prefix Inputs** — 각 입력 필드 **좌측에 아이콘/접두사**를 두고, 한 줄로 붙여서 하나의 입력 영역처럼 보이게 한 구조.

- 시안 1~11(단일·스플릿·글래스·인라인·벤토·다크·섹션·언더라인·필·투톤·좌 액센트)과 달리 **필드마다 접두사 영역**(@, 👤, 🔒)을 두어 입력 유형을 시각적으로 구분한다.
- 뷰포트는 가로·세로 중앙 정렬을 유지한다.

## UI/UX 특징

- **레이아웃**: 뷰포트 가로·세로 중앙 정렬. 카드 최대 너비 400px.
- **입력 래퍼**: `.iconform-input-wrap` = `.iconform-prefix`(min-width 44px, 배경 #f1f5f9) + input. 포커스 시 래퍼 border·ring primary.
- **접두사**: 이메일 @, 이름 👤, 비밀번호·확인 🔒. `aria-hidden="true"`로 스크린리더 중복 방지.
- **반응형**: 480px 이하에서 패딩·제목 크기 축소.
- **접근성**: 라벨–입력 연결, `aria-invalid`, 에러 메시지, 비밀번호 보기/숨기기.
- **애니메이션**: GSAP으로 `.iconform-field` 4개 stagger(0.05s), opacity + x 8, 0.35초.

## 사용 의도

- **필드 유형을 아이콘으로 구분**하고 싶을 때 쓰기 좋은 시안.
- HTMX `hx-post` 연동 가능, Alpine.js로 클라이언트 검증·토글.

## 구조

```
12/
├── index.html      # 회원가입 마크업 (HTMX + Alpine.js)
├── style.scss      # 시안 전용 SCSS
├── style.css       # style.scss 컴파일 결과
├── js/
│   ├── main.ts     # Alpine 데이터·폼 검증 (TypeScript)
│   └── main.js     # main.ts 컴파일 결과
└── README.md       # 본 설명
```

- **index.html**: `.signup-iconform` > 헤더 + form(.iconform-field × 4, 각각 .iconform-input-wrap > .iconform-prefix + input) + 가입하기 + 푸터.
- **style.scss / style.css**: prefix 영역 스타일, 강조색 #0f766e.
- **js/main.ts**: 시안 1~11과 동일 검증 로직, Alpine `signupForm`, GSAP 대상 `.iconform-field`(stagger, x 8).

## 기술 스택

- HTMX 1.9.x, Alpine.js 3.x, TypeScript, SASS/SCSS, GSAP 3.12
